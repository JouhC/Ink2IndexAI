# Ink2Index Frontend

Browser UI for the Ink2Index FastAPI backend.

## Run Locally

Start the backend from `backend/`:

```bash
uv run uvicorn app.main:app --reload
```

Start this frontend from `frontend/`:

```bash
npm run dev
```

Open:

```text
http://127.0.0.1:5173
```

